{
  "scripts": {
    "start": "platformatic start",
    "lint": "standard",
    "test": "pnpm run lint && borp --concurrency 1 --timeout=120000"
  },
  "devDependencies": {
    "@platformatic/db": "^2.56.0",
    "borp": "^0.19.0",
    "fastify": "^5.2.2",
    "standard": "^17.1.2"
  },
  "dependencies": {
    "@fastify/env": "^5.0.2",
    "@fastify/error": "^4.1.0",
    "fastify-plugin": "^5.0.1",
    "iovalkey": "^0.3.1"
  }
}
