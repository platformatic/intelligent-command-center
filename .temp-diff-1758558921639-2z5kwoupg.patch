diff --git a/test/install.bats b/test/install.bats
index 76591b0e..bad58389 100644
--- a/test/install.bats
+++ b/test/install.bats
@@ -351,65 +351,5 @@ setup() {
 	[[ "$output" =~ "--github-oauth-client-secret SECRET" ]]
 	[[ "$output" =~ "--google-oauth-client-id ID" ]]
 	[[ "$output" =~ "--google-oauth-client-secret SECRET" ]]
-	[[ "$output" =~ "--disable-icc-oauth" ]]
 	[[ "$output" =~ "OAuth Configuration:" ]]
-	[[ "$output" =~ "When OAuth is configured, demo authentication is automatically disabled" ]]
-	[[ "$output" =~ "Use --disable-icc-oauth to skip OAuth setup and use only demo authentication" ]]
-	[[ "$output" =~ "Demo authentication only (no OAuth):" ]]
-}
-
-# --disable-icc-oauth tests
-
-@test "install script accepts --disable-icc-oauth argument" {
-	run grep -n "disable-icc-oauth" "$INSTALL_SCRIPT"
-	[ "$status" -eq 0 ]
-}
-
-@test "install script has validate_oauth_conflicts function" {
-	run grep -n "validate_oauth_conflicts()" "$INSTALL_SCRIPT"
-	[ "$status" -eq 0 ]
-}
-
-@test "validate_oauth_conflicts detects conflict with GitHub OAuth ID" {
-	run bash -c "export DISABLE_ICC_OAUTH=true; export GITHUB_OAUTH_CLIENT_ID=test123; source '$INSTALL_SCRIPT'; validate_oauth_conflicts"
-	[ "$status" -eq 1 ]
-	[[ "$output" =~ "Cannot use --disable-icc-oauth with OAuth configuration flags" ]]
-	[[ "$output" =~ "--gh-oauth-client-id/--github-oauth-client-id" ]]
-}
-
-@test "validate_oauth_conflicts detects conflict with GitHub OAuth secret" {
-	run bash -c "export DISABLE_ICC_OAUTH=true; export GITHUB_OAUTH_CLIENT_SECRET=secret123; source '$INSTALL_SCRIPT'; validate_oauth_conflicts"
-	[ "$status" -eq 1 ]
-	[[ "$output" =~ "Cannot use --disable-icc-oauth with OAuth configuration flags" ]]
-	[[ "$output" =~ "--gh-oauth-client-secret/--github-oauth-client-secret" ]]
-}
-
-@test "validate_oauth_conflicts detects conflict with Google OAuth ID" {
-	run bash -c "export DISABLE_ICC_OAUTH=true; export GOOGLE_OAUTH_CLIENT_ID=google123; source '$INSTALL_SCRIPT'; validate_oauth_conflicts"
-	[ "$status" -eq 1 ]
-	[[ "$output" =~ "Cannot use --disable-icc-oauth with OAuth configuration flags" ]]
-	[[ "$output" =~ "--google-oauth-client-id" ]]
-}
-
-@test "validate_oauth_conflicts detects conflict with Google OAuth secret" {
-	run bash -c "export DISABLE_ICC_OAUTH=true; export GOOGLE_OAUTH_CLIENT_SECRET=googlesecret; source '$INSTALL_SCRIPT'; validate_oauth_conflicts"
-	[ "$status" -eq 1 ]
-	[[ "$output" =~ "Cannot use --disable-icc-oauth with OAuth configuration flags" ]]
-	[[ "$output" =~ "--google-oauth-client-secret" ]]
-}
-
-@test "validate_oauth_conflicts passes when only --disable-icc-oauth is set" {
-	run bash -c "export DISABLE_ICC_OAUTH=true; source '$INSTALL_SCRIPT'; validate_oauth_conflicts"
-	[ "$status" -eq 0 ]
-}
-
-@test "validate_oauth_conflicts passes when no flags are set" {
-	run bash -c "source '$INSTALL_SCRIPT'; validate_oauth_conflicts"
-	[ "$status" -eq 0 ]
-}
-
-@test "handle_oauth_configuration skips OAuth when disabled" {
-	run bash -c "export DISABLE_ICC_OAUTH=true; source '$INSTALL_SCRIPT'; handle_oauth_configuration"
-	[ "$status" -eq 0 ]
-	[[ "$output" =~ "ICC OAuth disabled, using demo authentication only" ]]
 }
